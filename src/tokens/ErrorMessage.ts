// The messages stay generic, in order the use them on multiple formfields

// TODO: add a variable to maxLength to specify the maximum amount of characters
export enum ErrorMessage {
  required = "Vul dit veld in",
  maxLength = "Dit veld overschrijdt het maximaal aantal karakters",
}
